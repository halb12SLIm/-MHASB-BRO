import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mhasb.pro',
  appName: 'محاسب سليم برو',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
